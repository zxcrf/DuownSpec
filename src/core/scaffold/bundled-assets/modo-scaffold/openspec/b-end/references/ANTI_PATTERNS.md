# B-End Anti-Patterns (禁忌)

以下为 B 端设计系统中严格禁止的模式：

1. **禁止使用毛玻璃或重度渐变** — B 端系统的工作台、表单、数据展示等核心操作区域追求信息密度和清晰度，毛玻璃效果会降低可读性。（注：登录页等品牌展示型页面**例外**，允许适度使用环境光模糊或弱渐变做装饰）。
2. **禁止隐藏主要操作按钮** — 所有核心业务操作必须始终可见，不允许嵌套在菜单深处。
3. **禁止在已有业务组件存在时重复造轮子** — 必须优先使用适配器中注册的业务组件，只有确认不存在时才允许基于基础组件新建。
