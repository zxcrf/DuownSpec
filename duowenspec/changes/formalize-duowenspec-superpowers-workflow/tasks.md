## 1. Enterprise Workflow Model

- [ ] 1.1 Add the canonical enterprise workflow description and diagram, including private-cloud verification and customer-environment verification before archive
- [x] 1.2 Encode stage ownership so DuowenSpec remains the workflow authority and superpowers remains stage-local augmentation
- [x] 1.3 Encode step criticality: `brainstorm` recommended when information is insufficient; `code`, `review`, `document`, and `release` required
- [x] 1.4 Define loop-back rules for validation failure, review failure, verification failure, documentation mismatch, private-cloud release failure, and customer-environment release failure
- [x] 1.5 Define the enterprise proposal structure so `Core User Stories` drive `Release Coverage` and release evidence planning
- [x] 1.6 Define the standard `Core User Stories` template and writing rules for enterprise proposals
- [x] 1.7 Define the standard `Release Coverage` template and writing rules for enterprise proposals
- [x] 1.8 Define the standard `Release Evidence Plan` template and record requirements for enterprise proposals
- [x] 1.9 Define how this fork packages enterprise behavior as its default published workflow
- [x] 1.10 Define the validation-repair path for existing changes without assuming artifact regeneration
- [x] 1.11 Define enterprise-first responsibilities for `duowenspec init`, `duowenspec config`, and `duowenspec update`

## 2. Reusable Product Extension

- [x] 2.1 Decide the reusable packaging mechanism in this fork: built-in enterprise profile, bundled enterprise skills, or both
- [x] 2.2 Add the reusable workflow assets to the current DuowenSpec codebase so downstream projects can adopt the workflow through `duowenspec init` without manual copying
- [x] 2.3 Ensure project setup/configuration can select and retain the enterprise workflow
- [x] 2.4 Update generated prompts/skills and guidance so downstream projects receive the same required/recommended stage rules
- [x] 2.4a Keep public workflow IDs unchanged while generating enterprise-oriented instructions by default in this fork
- [x] 2.4b Ensure init/config/update messaging and generation behavior consistently reflect enterprise-first defaults
- [x] 2.4c Update `src/core/global-config.ts` and `src/core/config-schema.ts` so this fork's defaults align with enterprise-first behavior
- [x] 2.4d Update `src/core/init.ts` so enterprise-first workflow artifacts are generated by default and success messaging reflects the enterprise next step
- [x] 2.4e Add packaged-skill preflight to `src/core/init.ts` before enterprise workflow artifacts are written
- [x] 2.4f Update `src/commands/config.ts` so configuration remains useful without presenting the lighter upstream workflow as a sibling in-fork mode
- [x] 2.4g Update `src/core/update.ts` so regeneration preserves enterprise-first behavior and clearly surfaces missing required capabilities
- [x] 2.5 Add an enterprise proposal template that extends the default proposal with `Core User Stories`, `Release Coverage`, `Release Evidence Plan`, and `Out of Scope`
- [x] 2.6 Ensure the enterprise proposal template includes fixed story fields and story-ID-based release coverage references
- [x] 2.7 Ensure the enterprise proposal template includes fixed private-cloud and customer-environment release coverage sections
- [x] 2.8 Ensure the enterprise proposal template includes fixed private-cloud and customer-environment evidence-planning sections
- [x] 2.9 Publish one complete enterprise `proposal.md` template that combines all required sections into a single authoring skeleton

## 3. Required Gates and Artifacts

- [x] 3.1 Decide whether `review`, `document`, and `release` should each have explicit tracked artifacts
- [ ] 3.2 If explicit artifacts are needed, add the smallest viable set of templates and dependencies
- [ ] 3.3 Ensure documentation completion is required before release
- [ ] 3.4 Ensure private-cloud verification is required before customer-environment verification
- [ ] 3.5 Ensure successful customer-environment verification is required before archive
- [ ] 3.6 Fold main-spec sync into enterprise archive with equivalent guarantees
- [x] 3.7 Decide what evidence must be recorded for each required gate
- [ ] 3.8 Ensure release validation scope is taken from the proposal's named user stories and covered flows, not invented during release
- [ ] 3.9 Ensure release records capture environment, method, outcome, covered stories, covered flows, evidence summary, and recorder identity
- [x] 3.10 Define the standard `review` record template and minimum required fields
- [x] 3.11 Define the standard documentation-completion record template and minimum required fields

## 4. Adoption and Publication

- [x] 4.1 Document how other projects should adopt the enterprise workflow from this fork
- [x] 4.1a Ensure enterprise setup performs packaged-skill preflight checks and blocks activation when workflow-referenced bundled enterprise skills are missing from the package
- [x] 4.2 Validate that a downstream project can enable the workflow without repo-specific manual setup
- [x] 4.2a Validate that this fork consistently generates enterprise workflow assets and points lighter-workflow users to official DuowenSpec
- [x] 4.2b Validate that init, config changes, and update all preserve enterprise-first behavior in generated artifacts
- [x] 4.2c Validate that `duowenspec init` installs workflow-referenced bundled enterprise skills for the selected workflow
- [x] 4.2d Validate that `duowenspec config` no longer advertises the lighter upstream path as a sibling in-fork mode
- [x] 4.2e Validate that `duowenspec update` preserves enterprise-first generated artifacts after configuration changes
- [x] 4.3 Document how project-specific exceptions should be declared in project-level `AGENTS.md`
- [ ] 4.4 Prepare release notes and publication guidance for shipping the enterprise workflow in this DuowenSpec derivative

## 5. Verification

- [x] 5.1 Validate this change with `duowenspec validate formalize-duowenspec-superpowers-workflow`
- [ ] 5.2 Walk through one representative change using the enterprise workflow and capture friction points
- [x] 5.3 Validate one downstream-adoption scenario using the workflow assets produced by this fork
- [ ] 5.4 Validate one exception scenario where a project documents an approved step variation in `AGENTS.md`
- [ ] 5.5 Update the workflow definition if the walkthroughs expose unclear ownership or missing gates
